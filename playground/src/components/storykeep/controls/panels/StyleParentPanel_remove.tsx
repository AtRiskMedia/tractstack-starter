import type { BasePanelProps } from "../SettingsPanel";

const StyleParentRemovePanel = ({ node, parentNode, layer, className }: BasePanelProps) => {
  return <div>ok</div>;
};

export default StyleParentRemovePanel;
