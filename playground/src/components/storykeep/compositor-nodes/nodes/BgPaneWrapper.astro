---
import BgPane from "../../../frontend/panes/BgPaneNew.astro";
import { getCtx } from "../../../../store/nodes";
import type { VisualBreakNode } from "../../../../types";

const { nodeId } = Astro.props;
const node = getCtx().allNodes.get().get(nodeId) as VisualBreakNode;
---

<BgPane payload={node} />
