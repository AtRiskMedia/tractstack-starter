---
import BgPane from "@/components/common/panes/BgPane.astro";
import { getCtx } from "@/store/nodes.ts";
import type { VisualBreakNode } from "@/types.ts";

const { nodeId } = Astro.props;
const node = getCtx().allNodes.get().get(nodeId) as VisualBreakNode;
---

<BgPane payload={node} />
