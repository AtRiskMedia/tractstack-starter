---
import { getCtx } from "../../../store/nodes";
import Node from "../../storykeep/compositor-nodes/Node.astro";

const { id } = Astro.props;
const rootId = id || getCtx().rootNodeId.get();
---

<Node nodeId={rootId} />
