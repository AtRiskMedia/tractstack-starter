---
import { rootNodeId } from "../../../store/nodes";
import Node from "../../storykeep/compositor-nodes/Node.astro";

const {slug} = Astro.props;
const rootId = rootNodeId.get();
---

<Node nodeId={rootId} />
